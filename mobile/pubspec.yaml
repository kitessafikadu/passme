name: mobile
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  get_it:
  font_awesome_flutter:
  google_fonts: ^6.1.0
  equatable: ^2.0.5
  dartz: ^0.10.1
  intl: ^0.18.1
  http: ^1.3.0
  fluttertoast: ^8.2.1
  shared_preferences: ^2.5.3
  flutter_dotenv: ^5.1.0
  record:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/questions_am.json
    - assets/questions_en.json
    - assets/images/google_icon.png
    - .env
